# Англи хэлний түвшин тогтоох шалгалтын веб

Байршууллаа: **http://localhost:3000**

## 📋 Функцүүд

### 🎯 Үндсэн урсгал
1. **Эхлэл хуудас** - Нэр, Нас, Email, Утас оруулна
2. **Тест** - 30 асуулт (10-аар хуудаслагдана), 60 минут timer
3. **Эссэ** - 2 эссэ (автомат хадгалалт 5 сек бүр)
4. **Үр дүн** - Оноо, түвшин, email илгээх

### ✨ Нэмэлт функцүүд
- ⏱️ **60 минутын timer** - хугацаа дуусмагц автомат шилжинэ
- 💾 **Автомат хадгалалт** - эссэ 5 сек бүр server рүү
- 📧 **Email илгээх** - түвшнийг хэрэглэгчид мэйл рүү явуулна
- 📊 **Admin Dashboard** - статистик, хэрэглэгч, асуулт засах
- 🔄 **10с автомат refresh** - админ хуудас шинэчлэгдэнэ

---

## 🚀 Суурилуулалт

```bash
cd /Users/mac/Documents/Shalgalt

# Сан суурилуулах
npm install

# Хөгжүүлэлтийн үе
npm run dev

# Үйлдвэрийн үе
npm start
```

**Сервер**: http://localhost:3000

---

## 🚀 Netlify дээр байршуулах

Энэ төслийг **Netlify Functions** архитектураар Netlify дээр үнэгүй байршуулж болно.

### Шаардлагатай:
- GitHub account + repository
- Netlify account (free tier)

### Хэрхэн:
1. [Netlify Dashboard](https://app.netlify.com) дээр нэвтрэх
2. "Add new site" → "Import an existing project"
3. GitHub Shalgalt repository сонгох
4. Build settings (автоматаар сэргээлэх):
   - **Build command**: `npm install`
   - **Publish directory**: `public`
5. Environment variables нэмэх (Site settings → Build & Deploy):
   ```
   SMTP_HOST=smtp.gmail.com
   SMTP_PORT=587
   SMTP_USER=your-email@gmail.com
   SMTP_PASS=your-app-password
   ```
6. Deploy дарна (автоматаар main branch-т push хийвэл update болно)

**Дэлгэрэнгүй нь**: `DEPLOYMENT_NETLIFY.md` файлыг үзнэ

---

## 📧 Email тохиргоо

`.env` файлд Gmail мэдээллээ оруулна (localhost-д):
```
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tanii-email@gmail.com
SMTP_PASS=your-app-password
```

Netlify дээр Environment variables-ээр тохирго хийнэ (дээр үзнэ).

---

## 📁 Файлын бүтэц

```
Shalgalt/
├── server.js              # Express сервер
├── package.json           # Сан
├── .env                   # Нууц мэдээлэл
├── src/
│   ├── store.js          # JSON data store
│   └── questions.json    # 30 асуулт
├── public/
│   ├── index.html        # Эхлэл
│   ├── test.html         # Тест (30 асуулт)
│   ├── essay1.html       # Эссэ 1
│   ├── essay2.html       # Эссэ 2
│   ├── result.html       # Үр дүн
│   ├── admin.html        # Админ панел
│   ├── questions.html    # Асуулт засах
│   ├── styles.css        # Загвар
│   └── js/               # Үйлчүүлэгчийн логик
└── data/
    └── db.json           # Өгөгдөл
```

---

## 🔐 API endpoints

### Хэрэглэгч
- `POST /api/users` - Нэмэх/шинэчлэх
- `GET /api/users` - Жагсаалт
- `PUT /api/users/:id` - Засах
- `DELETE /api/users/:id` - Устгах

### Тест
- `GET /api/questions` - Асуултууд
- `POST /api/tests/start` - Эхлүүлэх
- `POST /api/tests/:id/answers` - Хариулт
- `POST /api/tests/:id/essay1|essay2` - Эссэ
- `POST /api/tests/:id/finish` - Дуусгах
- `GET /api/tests/:id/result` - Үр дүн

### Асуулт (Админ)
- `GET /api/questions/admin` - Бүх асуулт
- `POST /api/questions` - Нэмэх/засах
- `DELETE /api/questions/:id` - Устгах

### Статистик
- `GET /api/stats` - Дундаж оноо, түвшин
- `GET /api/tests/all` - Бүх тестийн дүн

---

## 📊 Түвшин (CEFR)

| Оноо | Түвшин |
|------|--------|
| 28-30 | C1 - Мастер |
| 24-27 | B2 - Дээд дунд |
| 18-23 | B1 - Дунд |
| 11-17 | A2 - Элементар |
| 0-10 | A1 - Анхбеганр |

---

## 🎯 Ашиглалтын хэрэгцээ

### Оюутан
1. http://localhost:3000 нээнэ
2. Нэр, Нас, Email, Утас оруулна
3. "Тест эхлүүлэх" дарна
4. 30 асуулт 60 минутын хугацаатайгаар өгөнө
5. 2 эссэ бичнэ (автомат хадгалагдана)
6. "Дуусгах" дарна
7. Үр дүнгээ дэлгэцнээс харна, имэйлээр хүлээн авна
8. Email-ээр үр дүн ирнэ

### Админ
1. http://localhost:3000/admin.html нээнэ
2. **Статистик** - Нийт тест, дундаж оноо, түвшний задаргаа
3. **Хэрэглэгч** - Жагсаалт, засвар, устгалт
4. **Асуулт засах** - http://localhost:3000/questions.html

---

## ⚙️ Орчны хувьсагч (.env)

```
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=towshoo588@gmail.com
SMTP_PASS=vbvy foma pjbp dmsg
```

---

## 🐛 Алдаа засах

| Асуудал | Шийдэл |
|--------|--------|
| Асуултууд гарч ирэхгүй | `curl http://localhost:3000/api/questions` |
| Email явахгүй | `.env` файлыг шалгана |
| Timer ажиллахгүй | Browser console-т алдаа үзнэ |
| Админ дүнд гарч ирэхгүй | `/api/tests/all` endpoint шалгана |

---

## 📝 Сүүлийн өөрчлөлтүүд

✅ Асуултын UI сайжруулсан  
✅ Эссэ үг тоолох функц засасан  
✅ Email мэдээлэл .env-ээс уншина  
✅ Статистик dashboard нэмсэн  
✅ Асуулт засах интерфэйс нэмсэн  
✅ `.gitignore` нэмсэн  

---

**Сүүлийн шинэчлэл**: 2025-12-16
# Shalgalt1
